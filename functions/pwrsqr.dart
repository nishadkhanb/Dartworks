import 'dart:math';

void main() {
  int a = 10, b = 20, c = 2;
  num powernum = pow(a, c);
  num maxnum = max(a, b);
  num minnum = min(a, b);
  num squareroot = sqrt(b);
  print("power =$powernum");
  print("maxnum=$maxnum");
  print("minnum =$minnum");
  print("sqare root=$squareroot");
}
